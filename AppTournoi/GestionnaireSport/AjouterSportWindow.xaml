<Window x:Class="AppTournoi.GestionnaireSport.AjouterSportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppTournoi.GestionnaireSport"
        mc:Ignorable="d"
        Title="Ajouter un Sport" Height="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Nom:" Grid.Row="0" Grid.Column="0" Margin="10"/>
        <TextBox x:Name="NomTextBox" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <Button Content="Ajouter" Grid.Row="2" Grid.ColumnSpan="2" Margin="10" Click="AjouterButton_Click"/>
    </Grid>
</Window>
