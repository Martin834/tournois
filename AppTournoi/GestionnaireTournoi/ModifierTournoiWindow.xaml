<Window x:Class="AppTournoi.GestionnaireTournoi.ModifierTournoiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifier le Tournoi" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Nom:" Grid.Row="0" Grid.Column="0" Margin="10"/>
        <TextBox x:Name="NomTextBox" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <Label Content="Date:" Grid.Row="1" Grid.Column="0" Margin="10"/>
        <DatePicker x:Name="DatePicker" Grid.Row="1" Grid.Column="1" Margin="10"/>

        <Label Content="Sport:" Grid.Row="2" Grid.Column="0" Margin="10"/>
        <ComboBox x:Name="SportComboBox" Grid.Row="2" Grid.Column="1" Margin="10"/>

        <Button Content="Modifier" Grid.Row="3" Grid.ColumnSpan="2" Margin="10" Click="ModifierButton_Click"/>
    </Grid>
</Window>